export type Movie = {
	title: string;
	year: string;
	length: number;
	studioName: string;
};

export type Star = {
	name: string;
	address: string;
};

export type Studio = {
	name: string;
	address: string;
};

export type StarsIn = {
	title: string;
	starName: string;
	year: string;
};
